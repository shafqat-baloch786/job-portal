version: "3.9"

services:
  app:
    container_name: job-portal-app
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - ./utils/.env
    command: npm run dev
    restart: unless-stopped

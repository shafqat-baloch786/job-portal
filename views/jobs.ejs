<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Job Postings</title>
    
    <link rel="stylesheet" href="assets/css/bootstrap.min.css">
    <link rel="stylesheet" href="assets/css/font-awesome.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }

        .container {
            margin-top: 50px;
            max-width: 1300px;
            margin-left: auto;
            margin-right: auto;
        }

        h2 {
            font-weight: bold;
            color: #1f2b7b;
            text-align: center;
            margin-bottom: 40px;
        }

        .job-card {
            background: #ffffff;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            padding: 20px;
            height: 100%;
            margin-top: 10px;
            margin-bottom: 10px;
        }

        .job-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: #1f2b7b;
            margin-bottom: 15px;
        }

        .job-info {
            font-size: 1rem;
            color: #6c757d;
            margin-bottom: 10px;
        }

        .job-type {
            background: #1f2b7b;
            color: #fff;
            padding: 5px 10px;
            border-radius: 4px;
            font-weight: 600;
        }

        .salary {
            color: #1f2b7b;
            font-weight: 700;
            font-size: 1.1rem;
        }

        .description {
            margin-bottom: 15px;
            color: #555;
        }

        .apply-btn {
            background-color: #1f2b7b;
            color: white;
            padding: 10px 20px;
            font-weight: 600;
            text-align: center;
            border-radius: 5px;
            width: 100%;
            transition: background-color 0.3s ease;
        }

        .apply-btn:hover {
            background-color: #142071;
        }

        .job-card .d-flex {
            justify-content: space-between;
            align-items: center;
        }

        .job-card .apply-btn-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .job-card .apply-btn-container a {
            text-decoration: none;
            margin-top: 30px;
        }

       

        .badge-remote {
            background-color: #28a745;
            color: white;
            padding: 5px 10px;
            border-radius: 20px;
        }

        .badge-onsite {
            background-color: #f7a54b;
            color: white;
            padding: 5px 10px;
            border-radius: 20px;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <div class="container">
        <h2>Available Job Postings</h2>
        <div class="row">
            <% all_jobs.forEach(function(job) { %>
                <div class="col-md-4 mb-4">
                    <div class="job-card">
                        <h4 class="job-title"><%= job.title %></h4>
                        <p class="job-info"><strong>Company:</strong> <%= job.company %></p>
                        <p class="job-info"><strong>Location:</strong> <%= job.location %></p>
                        <p class="job-info"><strong>Position:</strong> <%= job.position %></p>
                        <p class="job-info"><strong>Experience Level:</strong> <%= job.experienceLevel %></p>
                        <p class="job-info"><strong>Job Posted by:</strong> <%= job.created_by %></p>
                        <p class="salary">Rs. <%= job.salary %></p>
                        <p class="description"><%= job.description %></p>
                        <div class="d-flex justify-content-between">
                            <span class="job-type"><%= job.jobType %></span>
                        </div>
                        <div class="apply-btn-container">
                            <a href="#" class="apply-btn">Apply Now</a>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
<!-- JS here -->
<script src="assets/js/vendor/jquery-1.12.4.min.js"></script>
<script src="assets/js/popper.min.js"></script>
<script src="assets/js/bootstrap.min.js"></script>

</body>
</html>
